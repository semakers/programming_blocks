import 'package:flutter/material.dart';

class CreationSectionItem extends StatelessWidget {
  const CreationSectionItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
